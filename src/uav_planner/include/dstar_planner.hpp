#ifndef __DSTAR_PLANNER__
#define __DSTAR_PLANNER__

#include <algorithm>
#include <priority_queue>
#include <pair>

class DStar {
  public:
    DStar();
    ~DStar();

  private:
    float calculateKey(float s);
    float calculateCost();
    void initialize()
    void 
};

#endif
